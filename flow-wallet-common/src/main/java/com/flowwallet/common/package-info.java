/**
 * FlowWallet Common module — shared contracts between microservices.
 * <p>
 * Contains DTOs, Kafka events, enums, constants, and the {@code @CurrentUserId} infrastructure.
 */
package com.flowwallet.common;
